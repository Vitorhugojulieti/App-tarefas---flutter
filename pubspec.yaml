name: projeto_despesas
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  intl: ^0.18.0
  shared_preferences: ^2.2.2
  provider: ^6.1.5+1
  table_calendar: ^3.0.9
  cupertino_icons: ^1.0.8
  timezone: ^0.8.0
  flutter_native_timezone: ^2.0.0
  flutter_local_notifications: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^5.0.0

# 🔥 CONFIG DO ÍCONE (fora de tudo)
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/task_icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/images/task_icon.png